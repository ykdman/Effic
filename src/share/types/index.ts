export type TContent = {
  name: string;
  path: string;
};

export type TTarget = {
  id: string;
  content: string;
};

export type TNote = {
  id: string;
  date: string;
  content: string;
  title: string;
};
